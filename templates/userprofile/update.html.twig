{% extends 'base.html.twig '%}
{% block title%} Modification des données {% endblock %}  
{% block body %}

salut
{#<div class="container">
<h1 class="mt-4">Modifier le profil</h1>
<div class="form-group">
{{form_label(form.username)}} 
{{form_widget(form.username)}} 
<div class="form-error">
{{form_errors(form.username)}}
</div>
</div>
<div class="form-group">
{{form_row(form.submit)}} 
</div> #}
{# {{form(form.email)}}  #}

      {# {{ form_start(form) }}  #}
        {# {{ form_widget(form) }} #}
        {# <button type="submit">Enregistrer</button> #}
    {# {{ form_end(form) }}  #}
{# {{form(form)}}   #}
 {% endblock %} 