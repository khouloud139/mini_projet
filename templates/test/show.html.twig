{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Distination</th>
                <td>{{ demande.Distination }}</td>
            </tr>
            <tr>
                <th>Lieudepart</th>
                <td>{{ demande.lieudepart }}</td>
            </tr>
            <tr>
                <th>Datesortie</th>
                <td>{{ demande.datesortie ? demande.datesortie|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Heuredepart</th>
                <td>{{ demande.heuredepart ? demande.heuredepart|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ demande.type }}</td>
            </tr>
            <tr>
                <th>Nombreplaces</th>
                <td>{{ demande.nombreplaces }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ demande.prix }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ demande.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_index') }}">back to list</a>

    <a href="{{ path('app_test_edit', {'id': demande.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
